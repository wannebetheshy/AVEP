services:
  frontend:
    build:
      dockerfile: Dockerfile.dev
      context: ./frontend
    env_file:
      - ./.env
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
